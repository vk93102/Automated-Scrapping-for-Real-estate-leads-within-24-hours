# Core scraper
requests
psycopg[binary]
pdf2image
pytesseract
pillow

# API server
fastapi
uvicorn[standard]
gunicorn

# LLM extraction (Groq — llama-3.3-70b-versatile)
# Falls back to rule-based extraction automatically when GROQ_API_KEY is not set.
groq

# HTML parsing for authenticated county assessor pages
beautifulsoup4

# Optional: only needed if using --search-backend playwright (not used in production)
# playwright

